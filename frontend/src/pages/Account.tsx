import {useMutation} from '@tanstack/react-query';
import {FormEvent,useState} from 'react';
import {useNavigate} from 'react-router-dom';
import {api} from '../api';
import {useAuth} from '../auth';
import {RoleLayout} from '../components/Shell';

export function Profile(){const{user,refresh}=useAuth();const[saved,setSaved]=useState('');const update=useMutation({mutationFn:(body:unknown)=>api.put('/profile',body),onSuccess:async()=>{await refresh();setSaved('Profile saved.')}});const profile=(user?.role==='tenant'?user.tenant_profile:user?.owner_profile)||{};const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);update.mutate({name:f.get('name'),phone:f.get('phone'),category:f.get('category')||null,institutionOrWorkplace:f.get('institutionOrWorkplace')||null,minBudget:Number(f.get('minBudget')||0)||null,maxBudget:Number(f.get('maxBudget')||0)||null,preferenceText:f.get('preferenceText')||null,requiredFacilities:[],businessName:f.get('businessName')||null,address:f.get('address')||null,verificationReference:f.get('verificationReference')||null})};return <RoleLayout role={user!.role}><div className="dash-head"><div><span className="eyebrow">Personal details</span><h1>Profile & preferences</h1></div></div><form className="form-card" onSubmit={submit}><div className="form">{saved&&<div className="notice notice-info">{saved}</div>}<div className="form-row"><label className="field">Full name<input className="input" name="name" required defaultValue={user?.name}/></label><label className="field">Mobile number<input className="input" name="phone" required pattern="(?:\+94|0)7\d{8}" defaultValue={user?.phone}/></label></div><label className="field">Email<input className="input" value={user?.email} disabled/><span className="field-help">Email changes require administrator support in this release.</span></label>{user?.role==='tenant'?<><div className="form-row"><label className="field">Category<select className="select" name="category" defaultValue={String(profile.category||'student')}><option value="student">Student</option><option value="worker">Worker</option><option value="other">Other</option></select></label><label className="field">Institution or workplace<input className="input" name="institutionOrWorkplace" defaultValue={String(profile.institution_or_workplace||'')}/></label></div><div className="form-row"><label className="field">Minimum budget<input className="input" name="minBudget" type="number" min="0" defaultValue={String(profile.min_budget_lkr||'')}/></label><label className="field">Maximum budget<input className="input" name="maxBudget" type="number" min="0" defaultValue={String(profile.max_budget_lkr||'')}/></label></div><label className="field">What matters to you?<textarea className="textarea" name="preferenceText" maxLength={1000} defaultValue={String(profile.preference_text||'')}/></label></>:<><label className="field">Business display name<input className="input" name="businessName" defaultValue={String(profile.business_name||'')}/></label><label className="field">Address<textarea className="textarea" name="address" defaultValue={String(profile.address||'')}/></label><label className="field">Academic verification reference<input className="input" name="verificationReference" defaultValue={String(profile.verification_reference||'')}/><span className="field-help">Use synthetic/demo references only. Never enter a real national identity number.</span></label><div className="notice notice-info">Verification status: {String(profile.verification_status||'pending')}</div></>}</div><div className="actions-row"><button className="btn btn-primary" disabled={update.isPending}>Save profile</button></div></form></RoleLayout>}

export function Security(){const{user,logout}=useAuth();const nav=useNavigate();const[message,setMessage]=useState('');const password=useMutation({mutationFn:(body:unknown)=>api.put('/account/password',body),onSuccess:()=>setMessage('Password changed and other sessions were signed out.')});const archive=useMutation({mutationFn:(value:string)=>api.delete('/account',{data:{password:value}}),onSuccess:async()=>{await logout().catch(()=>undefined);nav('/')}});const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);password.mutate({currentPassword:f.get('currentPassword'),password:f.get('password'),password_confirmation:f.get('password_confirmation')})};return <RoleLayout role={user!.role}><div className="dash-head"><div><span className="eyebrow">Account protection</span><h1>Security</h1></div></div><div className="dash-grid"><form className="form-card" onSubmit={submit}><h2>Change password</h2>{message&&<div className="notice notice-info">{message}</div>}<div className="form"><label className="field">Current password<input className="input" name="currentPassword" type="password" required/></label><label className="field">New password<input className="input" name="password" type="password" minLength={8} required/></label><label className="field">Confirm new password<input className="input" name="password_confirmation" type="password" minLength={8} required/></label></div><div className="actions-row"><button className="btn btn-primary">Update password</button></div></form><section className="panel"><h2>Account status</h2><p className="lead">Your account is <strong>{user?.status}</strong>. Archiving signs you out and removes access.</p>{user?.role!=='admin'&&<ArchiveAccount onArchive={value=>archive.mutate(value)}/>}</section></div></RoleLayout>}

function ArchiveAccount({onArchive}:{onArchive:(password:string)=>void}){const[open,setOpen]=useState(false);return <>{!open?<button className="btn btn-danger" onClick={()=>setOpen(true)}>Archive my account</button>:<form onSubmit={e=>{e.preventDefault();onArchive(String(new FormData(e.currentTarget).get('password')))}}><label className="field">Confirm with password<input className="input" type="password" name="password" required/></label><div className="modal-actions"><button type="button" className="btn btn-ghost" onClick={()=>setOpen(false)}>Cancel</button><button className="btn btn-danger">Confirm archive</button></div></form>}</>}
