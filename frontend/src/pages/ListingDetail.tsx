import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { type FormEvent, useEffect, useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { api } from '../api';
import { useAuth } from '../auth';
import { ListingCard } from '../components/ListingCard';
import { MapView } from '../components/MapView';
import { PublicLayout } from '../components/Shell';
import type { Listing } from '../types';

const money = (value: number) => new Intl.NumberFormat('en-LK', { style: 'currency', currency: 'LKR', maximumFractionDigits: 0 }).format(value).replace('LKR', 'Rs.');
const placeIcon: Record<string, string> = { bus_station: 'bi-bus-front', train_station: 'bi-train-front', supermarket: 'bi-cart3', hospital: 'bi-hospital', food: 'bi-cup-hot' };
type Detail = { data: Listing; favorite: boolean; reviews: Array<{ id: number; rating: number; body: string; tenant: { name: string } }>; reviewSummary: { summary: string; sampleSize: number; online?: boolean }; related: Listing[] };

export default function ListingDetail() {
  const { id } = useParams(); const { user } = useAuth(); const navigate = useNavigate(); const queryClient = useQueryClient();
  const [enquiring, setEnquiring] = useState(false); const [error, setError] = useState(''); const [saved, setSaved] = useState(false); const [saveMessage, setSaveMessage] = useState('');
  const { data, isLoading } = useQuery({ queryKey: ['listing', id], queryFn: async () => (await api.get(`/listings/${id}`)).data as Detail });
  useEffect(() => { if (data) setSaved(Boolean(data.favorite)); }, [data]);
  const review = useMutation({ mutationFn: (body: unknown) => api.post('/reviews', body), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['listing', id] }) });
  const favorite = useMutation({ mutationFn: () => saved ? api.delete(`/favorites/${id}`) : api.put(`/favorites/${id}`), onSuccess: response => { const next = Boolean(response.data.favorite); setSaved(next); setSaveMessage(next ? 'Saved to your shortlist.' : 'Removed from your shortlist.'); void queryClient.invalidateQueries({ queryKey: ['favorites'] }); }, onError: exception => setError((exception as { message?: string }).message || 'Could not update your shortlist.') });
  const enquiry = useMutation({ mutationFn: (text: string) => api.post('/conversations', { listingId: Number(id), text }), onSuccess: () => navigate('/tenant/messages') });
  if (isLoading || !data) return <PublicLayout><section className="sb-section"><div className="container sb-loading"><span className="spinner-border" /></div></section></PublicLayout>;
  const item = data.data;
  const submitReview = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const fields = new FormData(event.currentTarget); review.mutate({ listingId: item.id, rating: Number(fields.get('rating')), text: fields.get('text') }); };
  const startEnquiry = () => { if (!user) return navigate('/login'); if (user.role !== 'tenant') return setError('Only tenant accounts can start property enquiries.'); setEnquiring(true); };
  return <PublicLayout><section className="sb-detail-page"><div className="container"><nav className="sb-breadcrumb"><Link to="/">Home</Link><i className="bi bi-chevron-right" /><Link to="/search">Listings</Link><i className="bi bi-chevron-right" /><span>{item.slug}</span></nav><div className="row g-4"><div className="col-lg-8"><div className="sb-detail-gallery"><img src={item.image || item.images[0]?.url} alt={`Interior of ${item.title}`} /><span><i className="bi bi-patch-check-fill" /> Administrator verified</span></div><header className="sb-detail-header"><div><span className="sb-kicker">{item.propertyType.replaceAll('_', ' ')} · {item.genderRule.replaceAll('_', ' ')}</span><h1>{item.title}</h1><div className="sb-detail-meta"><span><i className="bi bi-geo-alt" /> {item.area}, {item.city}</span><span><i className="bi bi-star-fill" /> {item.rating} ({item.reviewCount} reviews)</span><span><i className="bi bi-people" /> Up to {item.occupancy}</span></div></div></header><section className="sb-detail-section"><h2>About this stay</h2><p>{item.description}</p></section><section className="sb-detail-section"><h2>Facilities included</h2><div className="sb-facility-grid">{item.facilities.map(facility => <span key={facility}><i className="bi bi-check2-circle" /> {facility}</span>)}</div></section>{item.nearbyPlaces?.length ? <section className="sb-detail-section"><div className="d-flex flex-wrap justify-content-between align-items-end gap-2 mb-4"><div><span className="sb-kicker">Neighbourhood intelligence</span><h2 className="mb-0">Daily essentials around this place</h2></div><small className="text-secondary">Straight-line distance from approximate map point</small></div><div className="sb-nearby-grid">{item.nearbyPlaces.map(place => <article key={place.type}><span><i className={`bi ${placeIcon[place.type] || 'bi-geo'}`} /></span><div><small>{place.type.replaceAll('_', ' ')}</small><strong>{place.name}</strong><b>{place.distanceM < 1000 ? `${place.distanceM} m` : `${(place.distanceM / 1000).toFixed(1)} km`}</b></div></article>)}</div></section> : null}<section className="sb-detail-section"><h2>Approximate public location</h2><p>The marker protects the street address while helping you understand the area. Confirm the route during a viewing.</p><MapView latitude={item.latitude} longitude={item.longitude} label={`${item.area}, ${item.city}`} /></section><section className="sb-detail-section"><h2>Resident experiences</h2><div className="sb-review-summary"><span><i className="bi bi-stars" /></span><div><strong>AI evidence summary</strong><p>{data.reviewSummary.summary}</p><small>Based on {data.reviewSummary.sampleSize} visible reviews. This summarizes opinions, not verified facts.</small></div></div>{data.reviews.map(entry => <article className="sb-review" key={entry.id}><div><strong>{entry.tenant?.name}</strong><span>{'★'.repeat(entry.rating)}</span></div><p>{entry.body}</p></article>)}{user?.role === 'tenant' && <form className="sb-review-form" onSubmit={submitReview}><h3>Share your experience</h3><div className="row g-3"><div className="col-md-3"><select className="form-select" name="rating"><option value="5">5 — Excellent</option><option value="4">4 — Good</option><option value="3">3 — Fair</option><option value="2">2 — Poor</option><option value="1">1 — Very poor</option></select></div><div className="col-md-9"><textarea className="form-control" aria-label="Review text" name="text" required minLength={15} maxLength={2000} placeholder="Describe cleanliness, safety, noise, owner response…" /></div></div><button className="btn sb-btn-primary mt-3">Publish or update review</button></form>}</section></div><div className="col-lg-4"><aside className="sb-booking-card"><span className="sb-availability"><i className="bi bi-circle-fill" /> Available {item.availableFrom}</span><div className="sb-booking-price">{money(item.price)}<small>per month</small></div>{item.deposit && <p><i className="bi bi-wallet2" /> Refundable deposit: {money(item.deposit)}</p>}{error && <div className="alert alert-warning" role="alert">{error}</div>}<button className="btn sb-btn-primary w-100 py-3" onClick={startEnquiry}><i className="bi bi-chat-dots me-2" />Send an enquiry</button>{user?.role === 'tenant' && <><button className={`btn sb-save-btn w-100 py-3 ${saved ? 'saved' : ''}`} aria-pressed={saved} disabled={favorite.isPending} onClick={() => favorite.mutate()}><i className={`bi ${saved ? 'bi-heart-fill' : 'bi-heart'}`} /> {favorite.isPending ? 'Updating…' : saved ? 'Saved — remove' : 'Save to favorites'}</button>{saveMessage && <div className="sb-inline-success" role="status"><i className="bi bi-check-circle-fill" /> {saveMessage}</div>}</>}<hr /><div className="sb-owner"><span>{item.ownerName?.slice(0, 2)}</span><div><small>Hosted by</small><strong>{item.ownerName}</strong><b><i className="bi bi-patch-check-fill" /> Verified owner</b></div></div><div className="sb-safety-note"><i className="bi bi-shield-check" /><span>View the property and verify the owner before making any direct payment.</span></div></aside></div></div>{data.related.length > 0 && <section className="sb-related"><div className="sb-section-head"><div><span className="sb-kicker">Continue exploring</span><h2>More around {item.city}</h2></div></div><div className="row g-4">{data.related.map(related => <div className="col-md-6 col-xl-4" key={related.id}><ListingCard item={related} /></div>)}</div></section>}</div></section>{enquiring && <div className="sb-modal-backdrop" onMouseDown={event => event.target === event.currentTarget && setEnquiring(false)}><form className="sb-modal" onSubmit={event => { event.preventDefault(); enquiry.mutate(String(new FormData(event.currentTarget).get('text'))); }}><button type="button" className="sb-modal-close" aria-label="Close" onClick={() => setEnquiring(false)}><i className="bi bi-x-lg" /></button><span className="sb-kicker">Private owner message</span><h2>Ask about {item.title}</h2><p>Your message is visible only to you and this listing’s owner.</p><textarea className="form-control" name="text" minLength={2} maxLength={2000} required placeholder="Ask about availability, a viewing or house rules…" /><div className="d-flex justify-content-end gap-2 mt-4"><button type="button" className="btn btn-outline-dark" onClick={() => setEnquiring(false)}>Cancel</button><button className="btn sb-btn-primary">Send securely</button></div></form></div>}</PublicLayout>;
}
